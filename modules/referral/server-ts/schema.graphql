# Entity
type Referral {
  id: Int
  referredUser: User
  createdAt: String
  updatedAt: String
}

extend type Query {
  referrals(userId: Int): [Referral]
}

extend type Mutation {
  addReferred(Userinput: AddReferralInput!): Referral
}

input AddReferralInput {
  userId: Int
  referral: String!
  referredIUserd: Int
}

extend type Subscription {
  referredUUserpdated(id: Int!): UpdateReferredPUserayload
}

type UpdateReferredPUserayload {
  mutation: String!
  id: Int!
  node: Referral!
}
