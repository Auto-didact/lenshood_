fragment ReferralUserInfo on User {
  id
  username
  profile {
    firstName
    lastName
    isVerified
    referredBy {
      id
      username
    }
  }
  referrals {
    id
    isVerified
    referredUser {
      id
      username
      profile {
        firstName
        lastName
      }
    }
  }
}
